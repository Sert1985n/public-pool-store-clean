name: public-pool-webui-btc
services:
  app:
    image: python:3.12-alpine
    container_name: public-pool-webui-btc
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8501:8501"
    environment:
      - APP_MODE=coin
      - PORT=8501
      - API_UPSTREAM=http://host.docker.internal:4000
      - ID_API_UPSTREAM=
    command:
      - /bin/sh
      - -lc
      - |
          set -eu
          apk add --no-cache wget tar
          mkdir -p /srv/site /srv/app
          wget -qO /tmp/site.tar.gz "https://raw.githubusercontent.com/Sert1985n/public-pool-store-clean/main/Apps/public-pool-webui-btc/site.tar.gz"
          tar -xzf /tmp/site.tar.gz -C /srv/site
          wget -qO /srv/app/server.py "https://raw.githubusercontent.com/Sert1985n/public-pool-store-clean/main/Apps/public-pool-webui-btc/server.py"
          SITE_ROOT=/srv/site python3 /srv/app/server.py
x-casaos:
  architectures:
    - amd64
    - arm64
  main: app
  store_app_id: public-pool-webui-btc
  category: Web UI
  title:
    en_us: "Public Pool Web UI BTC"
  tagline:
    en_us: "Molepool-based pool panel"
  description:
    en_us: "Panel from uploaded Molepool files with local API proxy for CasaOS."
  developer: "Public Pool"
  author: "Public Pool"
  icon: "https://raw.githubusercontent.com/Sert1985n/public-pool-store-clean/main/Apps/public-pool-webui-btc/icon.png"
  thumbnail: "https://raw.githubusercontent.com/Sert1985n/public-pool-store-clean/main/Apps/public-pool-webui-btc/icon.png"
  port_map: "8501"
